# Reporting

The reporting module provides aggregated views over monthly transactions. Current implementation includes category totals for a selected month.

Future improvements: currency conversion, date-range selection, charts, and exportable reports.
